val start : float -> unit -> float
(** [start bpm] is a thunk that starts the repeating metronome that
    plays a tick at a rate of [bpm] and is the float value of the
    current bpm of the metronome. *)
